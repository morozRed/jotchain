<%= turbo_stream.prepend "links_timeline", partial: "links/card", locals: { link: @link } %>
<%= turbo_stream.replace dom_id(@new_link, :form), partial: "links/form", locals: { chain: @chain, link: @new_link } %>
<%= turbo_stream.replace "flash_messages", partial: "shared/flash_container" %>
