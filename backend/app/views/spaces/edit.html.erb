<%= render layout: "shared/modal_wrapper", locals: { modal_title: "Edit Space", modal_size: "max-w-xl" } do %>
  <p class="mb-6 text-sm text-slate-400">
    Update your space name or description to better reflect what you're tracking.
  </p>
  <%= render "spaces/form", space: @space %>
<% end %>
