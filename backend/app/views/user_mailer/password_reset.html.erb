<h1 style="font-size: 24px; margin-bottom: 12px; color: #f8fafc; font-weight: 600; letter-spacing: -0.02em;">Reset your password</h1>
<p style="margin-bottom: 20px; color: rgba(226, 232, 240, 0.82);">
  We received a request to reset the password for <strong><%= @user.email %></strong>. Use the button below to choose a new one — the link expires in 20 minutes.
</p>

<%= render "mailers/shared/button", url: edit_identity_password_reset_url(sid: @signed_id), label: "Reset password" %>

<p style="margin-top: 24px; color: rgba(148, 163, 184, 0.85); font-size: 14px;">
  If you did not request this change, you can safely ignore this email. Your password will stay the same.
</p>
