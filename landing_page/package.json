{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "build:cf": "astro build --clear",
    "preview:cf": "wrangler pages dev ./dist",
    "deploy": "npx astro build --clear && echo '_worker.js' >> dist/.assetsignore && npx wrangler deploy"
  },
  "dependencies": {
    "astro": "5.15.3"
  }
}
