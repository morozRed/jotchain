---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const appSignupUrl = 'https://app.jotchain.com/sign_up';

const faqs = [
	{
		question: 'Is this task management?',
		answer: 'No. Jotchain sits beside tools like Jira and Linear — it doesn\'t replace them.',
	},
	{
		question: 'Do I need to write every day?',
		answer: 'No. A few notes per week is enough for patterns to emerge.',
	},
	{
		question: 'When do signals appear?',
		answer: 'Only after repetition. Jotchain stays quiet until there\'s evidence.',
	},
	{
		question: 'Can I export my data?',
		answer: 'Yes. Available on Pro.',
	},
];
---

<Layout
	title="JotChain · Your work leaves patterns"
	description="Jotchain observes short work notes over time and surfaces quiet signals about interruptions, unplanned work, and invisible impact — without metrics or pressure."
>
	<Header />

	<div class="page-wrapper">
		<!-- Hero Section -->
		<section class="hero">
			<div class="hero__content">
				<h1 class="fade-in fade-in-1">
					Your work leaves patterns —<br />even when tasks don't.
				</h1>
				<p class="hero__subtitle fade-in fade-in-2">
					Jotchain observes short work notes over time and surfaces quiet signals about interruptions, unplanned work, and invisible impact — without metrics or pressure.
				</p>
				<p class="hero__principle fade-in fade-in-3">
					Signals appear only after repetition.
				</p>
				<div class="hero__actions fade-in fade-in-4">
					<a class="button button--primary" href={appSignupUrl}>Start free</a>
					<a class="button button--secondary" href="#how-it-works">See how it works</a>
				</div>
			</div>

			<!-- App Screenshot Composition - Full width backdrop -->
			<div class="hero__visual">
				<div class="hero__screenshot-popup fade-in fade-in-4">
					<img
						src="/screenshots/entry_popup.png"
						alt="Quick note entry with automatic context tagging"
						loading="eager"
					/>
				</div>
				<div class="hero__screenshot-main fade-in fade-in-3">
					<img
						src="/screenshots/main_app_view.png"
						alt="Jotchain diagnostics view showing detected patterns in work"
						loading="eager"
					/>
				</div>
			</div>
		</section>

		<!-- Problem Section -->
		<section class="section problem">
			<div class="section__content">
				<span class="section__label">The reality</span>
				<h2>Work feels busy — but hard to explain.</h2>
				<p class="section__prose">
					Interruptions, blockers, and unplanned help rarely show up in task tools. Weeks pass, effort adds up, and when it matters — reviews, retros, check-ins — it's hard to say where your time and energy actually went.
				</p>
			</div>
		</section>

		<!-- The Shift Section -->
		<section class="section shift">
			<div class="shift__container">
				<div class="shift__content">
					<span class="section__label">A different approach</span>
					<h2>Planning tools track intent.<br/>Jotchain observes reality.</h2>
					<p class="section__prose">
						Most tools focus on what you planned to do. Jotchain helps you notice what actually happened. It doesn't optimize or score your work — it quietly observes patterns in what you write.
					</p>
				</div>
				<div class="shift__visual">
					<div class="shift__comparison">
						<div class="comparison__item comparison__item--faded">
							<span class="comparison__label">Planning tools</span>
							<div class="comparison__list">
								<span>What you intended</span>
								<span>Tasks & deadlines</span>
								<span>Status updates</span>
							</div>
						</div>
						<div class="comparison__divider">
							<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
								<path d="M12 5v14M5 12h14"/>
							</svg>
						</div>
						<div class="comparison__item comparison__item--active">
							<span class="comparison__label">Jotchain</span>
							<div class="comparison__list">
								<span>What actually happened</span>
								<span>Patterns over time</span>
								<span>Quiet observations</span>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- How It Works Section -->
		<section class="section how-it-works" id="how-it-works">
			<header class="section__header">
				<span class="section__label">How it works</span>
				<h2>A calm, system-first workflow</h2>
			</header>

			<div class="steps">
				<article class="step">
					<div class="step__number">1</div>
					<div class="step__content">
						<h3>Capture context</h3>
						<p>Jot short notes when something happens. No structure required.</p>
					</div>
				</article>

				<article class="step">
					<div class="step__number">2</div>
					<div class="step__content">
						<h3>Observe patterns</h3>
						<p>Jotchain watches for repetition over time.</p>
					</div>
				</article>

				<article class="step">
					<div class="step__number">3</div>
					<div class="step__content">
						<h3>Understand or ignore</h3>
						<p>When patterns emerge, you see a clear work-state readout. You can reflect — or not.</p>
					</div>
				</article>
			</div>
		</section>

		<!-- Signals Section -->
		<section class="section signals">
			<header class="section__header">
				<span class="section__label">What you'll notice</span>
				<h2>Quiet signals, not dashboards</h2>
			</header>

			<div class="signals__grid">
				<article class="signal">
					<div class="signal__icon">
						<svg width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5">
							<path d="M3 10h2M15 10h2M10 3v2M10 15v2"/>
							<circle cx="10" cy="10" r="4"/>
						</svg>
					</div>
					<h3>Interruptions</h3>
					<p>Frequent context switching that breaks focus.</p>
				</article>

				<article class="signal">
					<div class="signal__icon">
						<svg width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5">
							<path d="M7 10h6M10 7v6"/>
							<circle cx="10" cy="10" r="7"/>
						</svg>
					</div>
					<h3>Unplanned help</h3>
					<p>Time spent unblocking others that never shows up anywhere.</p>
				</article>

				<article class="signal">
					<div class="signal__icon">
						<svg width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5">
							<path d="M3 17h14M5 13l5-5 5 5"/>
						</svg>
					</div>
					<h3>Blockers & friction</h3>
					<p>Waiting, dependencies, and recurring slowdowns.</p>
				</article>
			</div>

			<p class="signals__note">
				Signals appear only when there's enough evidence — never on day one.
			</p>
		</section>

		<!-- Benefits Section -->
		<section class="section benefits">
			<div class="benefits__container">
				<div class="benefits__content">
					<span class="section__label">What this gives you</span>
					<h2>Clarity, without extra work</h2>
					<ul class="benefits__list">
						<li>
							<svg width="16" height="16" viewBox="0 0 16 16" fill="none">
								<path d="M3 8.5l3 3 7-7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
							</svg>
							<span>You see patterns, not just tasks</span>
						</li>
						<li>
							<svg width="16" height="16" viewBox="0 0 16 16" fill="none">
								<path d="M3 8.5l3 3 7-7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
							</svg>
							<span>Weekly updates become easier to write</span>
						</li>
						<li>
							<svg width="16" height="16" viewBox="0 0 16 16" fill="none">
								<path d="M3 8.5l3 3 7-7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
							</svg>
							<span>Conversations about work become concrete</span>
						</li>
					</ul>
					<p class="benefits__note">
						These are outcomes of observability — not goals you have to chase.
					</p>
				</div>
			</div>
		</section>

		<!-- Performance Summaries Section -->
		<section class="section summaries">
			<div class="summaries__container">
				<div class="summaries__content">
					<span class="section__label">When you need a summary</span>
					<h2>Your work, already written</h2>
					<p class="section__prose">
						Over time, Jotchain builds an understanding of how your work has unfolded. When it matters — performance reviews, retrospectives, or check-ins — you can generate a clear work summary based on real notes and observed patterns.
					</p>
					<p class="summaries__supporting">
						No templates to manage. No scores. No guesswork.
					</p>
				</div>
				<div class="summaries__visual">
					<div class="summary-preview">
						<div class="summary-preview__header">
							<span class="summary-preview__label">Generated Summary</span>
							<span class="summary-preview__date">Last 90 days</span>
						</div>
						<div class="summary-preview__content">
							<div class="summary-preview__line summary-preview__line--title"></div>
							<div class="summary-preview__line"></div>
							<div class="summary-preview__line"></div>
							<div class="summary-preview__line summary-preview__line--short"></div>
							<div class="summary-preview__spacer"></div>
							<div class="summary-preview__line summary-preview__line--title"></div>
							<div class="summary-preview__line"></div>
							<div class="summary-preview__line summary-preview__line--medium"></div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Trust Section -->
		<section class="section trust">
			<div class="trust__container">
				<header class="section__header">
					<span class="section__label">Philosophy</span>
					<h2>Built to stay calm</h2>
				</header>
				<div class="trust__grid">
					<div class="trust__item">
						<div class="trust__icon">
							<svg width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5">
								<rect x="4" y="8" width="12" height="9" rx="2"/>
								<path d="M7 8V5a3 3 0 016 0v3"/>
							</svg>
						</div>
						<span>Your notes stay private</span>
					</div>
					<div class="trust__item">
						<div class="trust__icon">
							<svg width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5">
								<path d="M4 10l4 4 8-8"/>
							</svg>
						</div>
						<span>No public metrics, no streaks, nothing is scored</span>
					</div>
					<div class="trust__item">
						<div class="trust__icon">
							<svg width="20" height="20" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5">
								<circle cx="10" cy="10" r="6"/>
								<path d="M10 7v3l2 2"/>
							</svg>
						</div>
						<span>Signals are observational, not evaluative</span>
					</div>
				</div>
				<p class="trust__note">
					Jotchain is designed to reduce pressure, not add it.
				</p>
			</div>
		</section>

		<!-- Pricing Section -->
		<section class="section pricing" id="pricing">
			<header class="section__header">
				<span class="section__label">Pricing</span>
				<h2>Simple and fair</h2>
			</header>

			<div class="pricing__cards">
				<article class="pricing-card">
					<header>
						<h3>Monthly</h3>
						<div class="pricing-card__price">
							<span class="pricing-card__amount">$9</span>
							<span class="pricing-card__period">/month</span>
						</div>
					</header>
					<p class="pricing-card__desc">14-day free trial</p>
					<ul>
						<li>Unlimited entries</li>
						<li>AI-powered signals</li>
						<li>Performance summaries</li>
						<li>Data export</li>
					</ul>
					<a class="button button--secondary" href={appSignupUrl}>Start free trial</a>
				</article>

				<article class="pricing-card pricing-card--highlighted">
					<header>
						<h3>Yearly</h3>
						<div class="pricing-card__price">
							<span class="pricing-card__amount">$90</span>
							<span class="pricing-card__period">/year</span>
						</div>
					</header>
					<p class="pricing-card__desc">Save $18 per year</p>
					<ul>
						<li>Unlimited entries</li>
						<li>AI-powered signals</li>
						<li>Performance summaries</li>
						<li>Data export</li>
					</ul>
					<a class="button button--primary" href={appSignupUrl}>Start free trial</a>
				</article>
			</div>
		</section>

		<!-- FAQ Section -->
		<section class="section faq">
			<header class="section__header">
				<h2>Questions</h2>
			</header>

			<div class="faq__list">
				{faqs.map((faq) => (
					<details class="faq__item">
						<summary>{faq.question}</summary>
						<p>{faq.answer}</p>
					</details>
				))}
			</div>
		</section>

		<!-- Final CTA -->
		<section class="section final-cta">
			<h2>See your work clearly.</h2>
			<p>No scores. No pressure.</p>
			<a class="button button--primary" href={appSignupUrl}>Start free</a>
		</section>
	</div>

	<Footer />
</Layout>

<style>
	.page-wrapper {
		width: 100%;
		max-width: var(--max-width);
		display: flex;
		flex-direction: column;
	}

	/* Section Label - shared component */
	.section__label {
		display: inline-block;
		font-family: var(--font-heading);
		font-size: 0.7rem;
		font-weight: 500;
		text-transform: uppercase;
		letter-spacing: 0.12em;
		color: var(--primary);
		margin-bottom: 1rem;
		position: relative;
		padding-left: 1.5rem;
	}

	.section__label::before {
		content: '';
		position: absolute;
		left: 0;
		top: 50%;
		transform: translateY(-50%);
		width: 1rem;
		height: 1px;
		background: var(--primary);
		opacity: 0.5;
	}

	/* Hero Section - Split layout with fixed screenshot area */
	.hero {
		position: relative;
		display: block;
		padding: 3rem 0 6rem;
		min-height: auto;
	}

	@media (min-width: 900px) {
		.hero {
			padding: 5rem 0 6rem;
			min-height: 520px;
		}
	}

	.hero__content {
		position: relative;
		z-index: 2;
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 0;
		max-width: 440px;
	}

	@media (min-width: 900px) {
		.hero__content {
			max-width: 380px;
		}
	}

	@media (min-width: 1100px) {
		.hero__content {
			max-width: 420px;
		}
	}

	.hero h1 {
		font-size: clamp(1.65rem, 3vw, 2.1rem);
		line-height: 1.3;
		letter-spacing: -0.02em;
		font-weight: 800;
		margin-bottom: 1.25rem;
	}

	.hero__subtitle {
		font-size: clamp(0.9rem, 1.4vw, 1rem);
		line-height: 1.7;
		color: var(--text-secondary);
		max-width: 400px;
		margin-bottom: 1rem;
	}

	.hero__principle {
		font-family: var(--font-heading);
		font-size: 0.7rem;
		font-weight: 500;
		letter-spacing: 0.02em;
		color: var(--text-muted);
		padding: 0.5rem 0;
		border-top: 1px solid var(--border-subtle);
		margin-bottom: 1.75rem;
		width: 100%;
	}

	.hero__actions {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.75rem;
	}

	/* Hero Visual - Absolutely positioned on the right */
	.hero__visual {
		position: relative;
		width: calc(100% + 3rem);
		margin-left: -1.5rem;
		margin-top: 2.5rem;
		perspective: 1400px;
		perspective-origin: 30% 50%;
	}

	@media (min-width: 900px) {
		.hero__visual {
			position: absolute;
			top: 50%;
			left: 44%;
			right: 0;
			transform: translateY(-50%);
			width: auto;
			max-width: none;
			margin: 0;
			z-index: 1;
			perspective: 1800px;
			perspective-origin: 0% 50%;
		}
	}

	@media (min-width: 1100px) {
		.hero__visual {
			left: 42%;
		}
	}

	@media (min-width: 1300px) {
		.hero__visual {
			left: 40%;
		}
	}

	/* Main screenshot - the diagnostics view */
	.hero__screenshot-main {
		position: relative;
		border-radius: 12px;
		overflow: hidden;
		background: var(--surface);
		transform-style: preserve-3d;
		transform: rotateY(-10deg) rotateX(5deg);
		box-shadow:
			0 0 0 1px rgba(0, 0, 0, 0.06),
			0 4px 8px rgba(0, 0, 0, 0.03),
			0 12px 24px rgba(0, 0, 0, 0.06),
			0 24px 48px rgba(0, 0, 0, 0.09),
			0 48px 96px rgba(0, 0, 0, 0.06);
	}

	.hero__screenshot-main img {
		display: block;
		width: 100%;
		height: auto;
	}

	/* Popup screenshot - floating on the RIGHT bottom */
	.hero__screenshot-popup {
		position: absolute;
		z-index: 3;
		bottom: -5%;
		left: 50%;
		width: 45%;
		max-width: 340px;
		border-radius: 12px;
		overflow: hidden;
		background: var(--surface);
		transform-style: preserve-3d;
		transform: rotateY(-6deg) rotateX(3deg) translateZ(60px);
		box-shadow:
			0 0 0 1px rgba(0, 0, 0, 0.05),
			0 8px 16px rgba(0, 0, 0, 0.06),
			0 20px 40px rgba(0, 0, 0, 0.1),
			0 40px 80px rgba(0, 0, 0, 0.08);
	}

	.hero__screenshot-popup img {
		display: block;
		width: 100%;
		height: auto;
	}

	/* Staggered entrance animations */
	.hero__screenshot-main.fade-in {
		animation: mainReveal 1s cubic-bezier(0.22, 1, 0.36, 1) forwards;
		animation-delay: 150ms;
	}

	.hero__screenshot-popup.fade-in {
		animation: popupReveal 0.9s cubic-bezier(0.22, 1, 0.36, 1) forwards;
		animation-delay: 450ms;
	}

	@keyframes mainReveal {
		from {
			opacity: 0;
			transform: rotateY(-14deg) rotateX(7deg) translateY(40px);
		}
		to {
			opacity: 1;
			transform: rotateY(-10deg) rotateX(5deg) translateY(0);
		}
	}

	@keyframes popupReveal {
		from {
			opacity: 0;
			transform: rotateY(-10deg) rotateX(5deg) translateZ(30px) translateY(30px);
		}
		to {
			opacity: 1;
			transform: rotateY(-6deg) rotateX(3deg) translateZ(60px) translateY(0);
		}
	}

	/* Mobile adjustments */
	@media (max-width: 899px) {
		.hero__screenshot-main {
			transform: rotateY(-6deg) rotateX(4deg);
		}

		.hero__screenshot-popup {
			bottom: -8%;
			right: 2%;
			width: 48%;
			max-width: 260px;
			transform: rotateY(-4deg) rotateX(2deg) translateZ(40px);
		}

		@keyframes mainReveal {
			from {
				opacity: 0;
				transform: rotateY(-10deg) rotateX(6deg) translateY(30px);
			}
			to {
				opacity: 1;
				transform: rotateY(-6deg) rotateX(4deg) translateY(0);
			}
		}

		@keyframes popupReveal {
			from {
				opacity: 0;
				transform: rotateY(-8deg) rotateX(4deg) translateZ(20px) translateY(20px);
			}
			to {
				opacity: 1;
				transform: rotateY(-4deg) rotateX(2deg) translateZ(40px) translateY(0);
			}
		}
	}

	@media (max-width: 600px) {
		.hero__screenshot-popup {
			width: 52%;
			max-width: 220px;
			right: 0;
			bottom: -5%;
		}
	}

	/* Problem Section */
	.problem {
		padding: 6rem 0;
		text-align: center;
		border-top: 1px solid var(--border-subtle);
	}

	.section__content {
		max-width: 640px;
		margin: 0 auto;
	}

	.problem h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
		line-height: 1.2;
		margin-bottom: 1.5rem;
	}

	.section__prose {
		font-size: 1.1rem;
		line-height: 1.75;
		color: var(--text-secondary);
	}

	/* The Shift Section */
	.shift {
		padding: 6rem 0;
		background: var(--subtle);
		width: 100vw;
		margin-left: 50%;
		transform: translateX(-50%);
		border-top: 1px solid var(--border-subtle);
		border-bottom: 1px solid var(--border-subtle);
	}

	.shift__container {
		max-width: var(--max-width);
		margin: 0 auto;
		padding: 0 1.5rem;
		display: grid;
		gap: 4rem;
		align-items: center;
	}

	@media (min-width: 900px) {
		.shift__container {
			grid-template-columns: 1fr 1fr;
			gap: 4rem;
			padding: 0 2.5rem;
		}
	}

	.shift__content h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
		line-height: 1.25;
		margin-bottom: 1.5rem;
	}

	.shift__visual {
		display: flex;
		justify-content: center;
	}

	.shift__comparison {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		width: 100%;
		max-width: 360px;
	}

	@media (min-width: 500px) {
		.shift__comparison {
			flex-direction: row;
			gap: 1rem;
			max-width: none;
		}
	}

	.comparison__item {
		flex: 1;
		padding: 1.5rem;
		border-radius: var(--radius);
		background: var(--surface);
		border: 1px solid var(--border-subtle);
	}

	.comparison__item--faded {
		opacity: 0.6;
	}

	.comparison__item--active {
		border-color: var(--primary);
		box-shadow: 0 0 0 3px var(--primary-soft-bg);
	}

	.comparison__label {
		display: block;
		font-family: var(--font-heading);
		font-size: 0.7rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		color: var(--text-muted);
		margin-bottom: 1rem;
	}

	.comparison__item--active .comparison__label {
		color: var(--primary);
	}

	.comparison__list {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.comparison__list span {
		font-size: 0.9rem;
		color: var(--text-secondary);
	}

	.comparison__divider {
		display: none;
		align-items: center;
		justify-content: center;
		color: var(--text-muted);
		transform: rotate(90deg);
	}

	@media (min-width: 500px) {
		.comparison__divider {
			display: flex;
			transform: none;
		}
	}

	/* How It Works Section */
	.how-it-works {
		padding: 6rem 0;
	}

	.how-it-works .section__header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.how-it-works .section__header h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
		margin-top: 0.5rem;
	}

	.steps {
		display: flex;
		flex-direction: column;
		gap: 2rem;
		max-width: 640px;
		margin: 0 auto;
	}

	@media (min-width: 768px) {
		.steps {
			flex-direction: row;
			gap: 3rem;
			max-width: none;
		}
	}

	.step {
		flex: 1;
		display: flex;
		flex-direction: column;
		gap: 1rem;
		text-align: center;
	}

	@media (min-width: 768px) {
		.step {
			text-align: left;
		}
	}

	.step__number {
		width: 2.5rem;
		height: 2.5rem;
		border-radius: 50%;
		background: var(--primary-soft-bg);
		border: 1px solid var(--primary-soft-border);
		display: flex;
		align-items: center;
		justify-content: center;
		font-family: var(--font-heading);
		font-size: 0.85rem;
		font-weight: 600;
		color: var(--primary);
		margin: 0 auto;
	}

	@media (min-width: 768px) {
		.step__number {
			margin: 0;
		}
	}

	.step__content h3 {
		font-size: 1.125rem;
		margin-bottom: 0.5rem;
	}

	.step__content p {
		font-size: 0.95rem;
		line-height: 1.6;
		color: var(--text-secondary);
	}

	/* Signals Section */
	.signals {
		padding: 6rem 0;
		background: var(--subtle);
		width: 100vw;
		margin-left: 50%;
		transform: translateX(-50%);
		border-top: 1px solid var(--border-subtle);
	}

	.signals .section__header {
		text-align: center;
		max-width: var(--max-width);
		margin: 0 auto 3rem;
		padding: 0 1.5rem;
	}

	.signals .section__header h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
		margin-top: 0.5rem;
	}

	.signals__grid {
		display: grid;
		gap: 1.5rem;
		max-width: var(--max-width);
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	@media (min-width: 768px) {
		.signals__grid {
			grid-template-columns: repeat(3, 1fr);
			gap: 2rem;
			padding: 0 2.5rem;
		}
	}

	.signal {
		background: var(--surface);
		border: 1px solid var(--border-subtle);
		border-radius: var(--radius);
		padding: 1.75rem;
		text-align: center;
	}

	@media (min-width: 768px) {
		.signal {
			text-align: left;
		}
	}

	.signal__icon {
		width: 2.5rem;
		height: 2.5rem;
		border-radius: 0.625rem;
		background: var(--primary-soft-bg);
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--primary);
		margin: 0 auto 1rem;
	}

	@media (min-width: 768px) {
		.signal__icon {
			margin: 0 0 1rem 0;
		}
	}

	.signal h3 {
		font-size: 1rem;
		margin-bottom: 0.5rem;
	}

	.signal p {
		font-size: 0.9rem;
		line-height: 1.6;
		color: var(--text-secondary);
	}

	.signals__note {
		text-align: center;
		font-size: 0.9rem;
		color: var(--text-muted);
		max-width: var(--max-width);
		margin: 2rem auto 0;
		padding: 0 1.5rem;
		font-style: italic;
	}

	/* Benefits Section */
	.benefits {
		padding: 6rem 0;
		border-top: 1px solid var(--border-subtle);
	}

	.benefits__container {
		max-width: 560px;
		margin: 0 auto;
	}

	.benefits__content h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
		margin-bottom: 2rem;
	}

	.benefits__list {
		list-style: none;
		padding: 0;
		margin: 0 0 1.5rem;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.benefits__list li {
		display: flex;
		align-items: center;
		gap: 0.875rem;
		font-size: 1.05rem;
		color: var(--text-primary);
	}

	.benefits__list svg {
		flex-shrink: 0;
		color: var(--primary);
	}

	.benefits__note {
		font-size: 0.9rem;
		color: var(--text-muted);
		font-style: italic;
		padding-left: 1.875rem;
	}

	/* Summaries Section */
	.summaries {
		padding: 6rem 0;
		background: var(--subtle);
		width: 100vw;
		margin-left: 50%;
		transform: translateX(-50%);
		border-top: 1px solid var(--border-subtle);
	}

	.summaries__container {
		max-width: var(--max-width);
		margin: 0 auto;
		padding: 0 1.5rem;
		display: grid;
		gap: 3rem;
		align-items: center;
	}

	@media (min-width: 900px) {
		.summaries__container {
			grid-template-columns: 1fr 1fr;
			gap: 4rem;
			padding: 0 2.5rem;
		}
	}

	.summaries__content h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
		margin-bottom: 1.5rem;
	}

	.summaries__supporting {
		margin-top: 1.5rem;
		font-size: 0.95rem;
		color: var(--text-muted);
		font-style: italic;
	}

	.summaries__visual {
		display: flex;
		justify-content: center;
	}

	.summary-preview {
		width: 100%;
		max-width: 320px;
		background: var(--surface);
		border: 1px solid var(--border-subtle);
		border-radius: var(--radius);
		padding: 1.5rem;
		box-shadow: var(--shadow-md);
	}

	.summary-preview__header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		margin-bottom: 1.25rem;
		padding-bottom: 1rem;
		border-bottom: 1px solid var(--border-subtle);
	}

	.summary-preview__label {
		font-family: var(--font-heading);
		font-size: 0.7rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		color: var(--primary);
	}

	.summary-preview__date {
		font-size: 0.75rem;
		color: var(--text-muted);
	}

	.summary-preview__content {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.summary-preview__line {
		height: 0.625rem;
		background: var(--border-subtle);
		border-radius: 0.25rem;
	}

	.summary-preview__line--title {
		width: 45%;
		background: var(--border);
	}

	.summary-preview__line--short {
		width: 60%;
	}

	.summary-preview__line--medium {
		width: 75%;
	}

	.summary-preview__spacer {
		height: 0.75rem;
	}

	/* Trust Section */
	.trust {
		padding: 6rem 0;
		border-top: 1px solid var(--border-subtle);
	}

	.trust__container {
		max-width: 640px;
		margin: 0 auto;
	}

	.trust .section__header {
		text-align: center;
		margin-bottom: 3rem;
	}

	.trust .section__header h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
		margin-top: 0.5rem;
	}

	.trust__grid {
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
	}

	.trust__item {
		display: flex;
		align-items: center;
		gap: 1rem;
		padding: 1rem 1.25rem;
		background: var(--subtle);
		border-radius: var(--radius-sm);
	}

	.trust__icon {
		flex-shrink: 0;
		width: 2.25rem;
		height: 2.25rem;
		border-radius: 0.5rem;
		background: var(--surface);
		border: 1px solid var(--border-subtle);
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--primary);
	}

	.trust__item span {
		font-size: 0.95rem;
		color: var(--text-primary);
	}

	.trust__note {
		text-align: center;
		font-size: 0.9rem;
		color: var(--text-muted);
		margin-top: 2rem;
		font-style: italic;
	}

	/* Pricing Section */
	.pricing {
		padding: 6rem 0;
		background: var(--subtle);
		width: 100vw;
		margin-left: 50%;
		transform: translateX(-50%);
		border-top: 1px solid var(--border-subtle);
	}

	.pricing .section__header {
		text-align: center;
		max-width: var(--max-width);
		margin: 0 auto 3rem;
		padding: 0 1.5rem;
	}

	.pricing .section__header h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
		margin-top: 0.5rem;
	}

	.pricing__cards {
		display: grid;
		gap: 1.5rem;
		max-width: 600px;
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	@media (min-width: 640px) {
		.pricing__cards {
			grid-template-columns: repeat(2, 1fr);
			padding: 0 2.5rem;
		}
	}

	.pricing-card {
		padding: 2rem;
		background: var(--surface);
		border: 1px solid var(--border-subtle);
		border-radius: var(--radius);
		display: flex;
		flex-direction: column;
	}

	.pricing-card--highlighted {
		border-color: var(--primary);
		position: relative;
	}

	.pricing-card--highlighted::before {
		content: 'Best value';
		position: absolute;
		top: -0.75rem;
		left: 1.5rem;
		background: var(--primary);
		color: var(--primary-foreground);
		font-family: var(--font-heading);
		font-size: 0.6rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		padding: 0.375rem 0.75rem;
		border-radius: var(--radius-sm);
	}

	.pricing-card header {
		margin-bottom: 0.5rem;
	}

	.pricing-card h3 {
		font-family: var(--font-body);
		font-size: 1rem;
		font-weight: 600;
		margin-bottom: 0.25rem;
	}

	.pricing-card__price {
		display: flex;
		align-items: baseline;
		gap: 0.125rem;
	}

	.pricing-card__amount {
		font-family: var(--font-heading);
		font-size: 2.25rem;
		font-weight: 600;
		color: var(--text-primary);
	}

	.pricing-card__period {
		font-size: 0.9rem;
		color: var(--text-muted);
	}

	.pricing-card__desc {
		font-size: 0.875rem;
		color: var(--text-secondary);
		margin-bottom: 1.5rem;
	}

	.pricing-card ul {
		list-style: none;
		margin: 0 0 1.5rem;
		padding: 0;
		display: flex;
		flex-direction: column;
		gap: 0.625rem;
		flex: 1;
	}

	.pricing-card li {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		font-size: 0.9rem;
		color: var(--text-secondary);
	}

	.pricing-card li::before {
		content: '';
		width: 4px;
		height: 4px;
		border-radius: 50%;
		background: var(--primary);
		opacity: 0.6;
	}

	.pricing-card .button {
		width: 100%;
		justify-content: center;
	}

	/* FAQ Section */
	.faq {
		padding: 6rem 0;
		border-top: 1px solid var(--border-subtle);
	}

	.faq .section__header {
		text-align: center;
		margin-bottom: 3rem;
	}

	.faq .section__header h2 {
		font-size: clamp(1.5rem, 3vw, 2rem);
	}

	.faq__list {
		max-width: 640px;
		margin: 0 auto;
		display: flex;
		flex-direction: column;
		gap: 0;
	}

	.faq__item {
		border-bottom: 1px solid var(--border-subtle);
	}

	.faq__item:first-child {
		border-top: 1px solid var(--border-subtle);
	}

	.faq__item summary {
		padding: 1.25rem 0;
		font-size: 1rem;
		font-weight: 500;
		color: var(--text-primary);
		cursor: pointer;
		list-style: none;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
	}

	.faq__item summary::-webkit-details-marker {
		display: none;
	}

	.faq__item summary::after {
		content: '+';
		font-family: var(--font-heading);
		font-size: 1.25rem;
		font-weight: 400;
		color: var(--text-muted);
		transition: transform 0.2s ease;
	}

	.faq__item[open] summary::after {
		transform: rotate(45deg);
	}

	.faq__item p {
		padding: 0 0 1.25rem;
		font-size: 0.95rem;
		line-height: 1.7;
		color: var(--text-secondary);
	}

	/* Final CTA Section */
	.final-cta {
		text-align: center;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.75rem;
		padding: 6rem 1.5rem 8rem;
		background: linear-gradient(
			180deg,
			var(--background) 0%,
			var(--primary-soft-bg) 100%
		);
		width: 100vw;
		margin-left: 50%;
		transform: translateX(-50%);
		max-width: none;
		border-top: 1px solid var(--border-subtle);
	}

	.final-cta h2 {
		font-size: clamp(1.5rem, 3vw, 2.25rem);
	}

	.final-cta p {
		color: var(--text-secondary);
		font-size: 1.05rem;
	}

	.final-cta .button {
		margin-top: 0.75rem;
		padding: 0.875rem 2.25rem;
	}
</style>
